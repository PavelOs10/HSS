Структура моего проекта сейчас
venv/
art_school/
├──art_school/
│   ├──__init__.py
│   ├──asgi.py
│   ├──settings.py
│   ├──urls.py
│   ├──wsgi.py
├── main/
│   ├──migrations/
│   ├──__init__.py
│   ├──admin.py
│   ├──apps.py
│   ├──forms.py
│   ├──models.py
│   ├──tests.py
│   └──views.py
├──media/
│   └──news_images/
├──static/
│   ├── css/
│   │   ├── style.css
│   │   ├── news.css
│   │   ├── photo_reports.css
│   │   └── reviews.css
│   ├── fonts/
│   ├── img/
│   └── js/
│        └── script.js
├── templates/
│    └── main/
│         ├── base.html
│         ├── index.html
│         ├── news.html
│         ├── photo_reports.html
│         └── reviews.html
├── db.sqlite3
└── manage.py

asgi.py

"""
ASGI config for art_school project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/5.1/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'art_school.settings')

application = get_asgi_application()


settings.py
from pathlib import Path
import os

BASE_DIR = Path(__file__).resolve().parent.parent

SECRET_KEY = 'ваш_секретный_ключ'
DEBUG = True
ALLOWED_HOSTS = ['*']

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'main',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'art_school.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

STATIC_URL = '/static/'
STATICFILES_DIRS = [os.path.join(BASE_DIR, 'static')]

LANGUAGE_CODE = 'ru-ru'
TIME_ZONE = 'Europe/Moscow'
USE_I18N = True
USE_TZ = True

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

urls.py
from django.contrib import admin
from django.urls import path, include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),  # Админка Django
    path('', include('main.urls')),  # Подключение маршрутов приложения main
]

# Маршруты для медиафайлов в режиме разработки
if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)

wsgi.py
"""
WSGI config for art_school project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/5.1/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'art_school.settings')

application = get_wsgi_application()


admin.py

from django.contrib import admin
from .models import Review, News, PhotoReport

@admin.register(Review)
class ReviewAdmin(admin.ModelAdmin):
    list_display = ("name", "created_at")
    search_fields = ("name", "text")


@admin.register(News)
class NewsAdmin(admin.ModelAdmin):
    list_display = ("title", "created_at")
    search_fields = ("title", "content")


@admin.register(PhotoReport)
class PhotoReportAdmin(admin.ModelAdmin):
    list_display = ("title", "created_at")
    search_fields = ("title", "description")

apps.py
from django.apps import AppConfig


class MainConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'main'
forms.py
from django import forms
from .models import Review

class ReviewForm(forms.ModelForm):
    class Meta:
        model = Review
        fields = ["name", "text"]
        widgets = {
            "name": forms.TextInput(attrs={"class": "form-control", "placeholder": "Ваше имя"}),
            "text": forms.Textarea(attrs={"class": "form-control", "placeholder": "Ваш отзыв"}),
        }

models.py
from django.db import models

class Review(models.Model):
    name = models.CharField(max_length=100, verbose_name="Имя")
    text = models.TextField(verbose_name="Текст отзыва")
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="Дата создания")

    def __str__(self):
        return f"{self.name} - {self.created_at}"

    class Meta:
        verbose_name = "Отзыв"
        verbose_name_plural = "Отзывы"


class News(models.Model):
    title = models.CharField(max_length=200, verbose_name="Заголовок")
    content = models.TextField(verbose_name="Содержание")
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="Дата создания")
    image = models.ImageField(upload_to="news_images/", verbose_name="Изображение", null=True, blank=True)

    def __str__(self):
        return self.title

    class Meta:
        verbose_name = "Новость"
        verbose_name_plural = "Новости"


# main/models.py
class PhotoReport(models.Model):
    title = models.CharField(max_length=200)
    description = models.TextField()
    created_at = models.DateTimeField(auto_now_add=True)
    main_image = models.ImageField(upload_to='photo_reports/')

class ReportImage(models.Model):
    report = models.ForeignKey(PhotoReport, related_name='images', on_delete=models.CASCADE)
    image = models.ImageField(upload_to='photo_reports/')

urls.py
from django.urls import path
from . import views

urlpatterns = [
    path("", views.index, name="index"),
    path("reviews/", views.reviews, name="reviews"),
    path('news/<int:pk>/', views.news_detail, name='news_detail'),
    path('photo_reports/<int:pk>/carousel/', views.photo_report_detail, name='photo_report_detail'),
]

views.py
from django.shortcuts import render, redirect  # Добавьте redirect здесь
from .models import Review, News, PhotoReport
from .forms import ReviewForm

def index(request):
    return render(request, "main/index.html")

def reviews(request):
    reviews = Review.objects.all().order_by("-created_at")
    if request.method == "POST":
        form = ReviewForm(request.POST)
        if form.is_valid():
            form.save()
            return redirect("reviews")  # Перенаправление на страницу отзывов
    else:
        form = ReviewForm()
    return render(request, "main/reviews.html", {"reviews": reviews, "form": form})

def news(request):
    news = News.objects.all().order_by("-created_at")
    return render(request, "main/news.html", {"news": news})

def photo_report_detail(request, pk):
    report = get_object_or_404(PhotoReport, pk=pk)
    return render(request, 'main/photo_carousel.html', {'report': report})

def news_detail(request, pk):
    news_item = get_object_or_404(News, pk=pk)
    return render(request, 'main/news_detail.html', {'news_item': news_item})

news.css
/* static/css/news.css */

.news-card {
    cursor: pointer;
    transition: all 0.3s ease;
    border: none;
    border-radius: 15px;
    overflow: hidden;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
}

.news-image {
    height: 250px;
    object-fit: cover;
}

.modal-content {
    border-radius: 15px;
    background: rgba(255, 255, 255, 0.95);
}

.modal-body {
    padding: 30px;
}

photo_reports.css
/* static/css/photo_reports.css */

.photo-report-card {
    border: none;
    border-radius: 15px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(5px);
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease;
}

.photo-report-card.show {
    opacity: 1;
    transform: translateY(0);
}

.photo-report-card:hover {
    transform: translateY(-5px) scale(1.02);
    box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
}

reviews.py
/* static/css/reviews.css */

.review-card {
    background: rgba(255, 255, 255, 0.9);
    padding: 20px;
    border-radius: 15px;
    margin: 20px auto;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    min-height: 250px;
    /* Увеличиваем высоту */
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease;
}

.carousel-control-prev {
    left: -50px;
}

.carousel-control-next {
    right: -50px;
}

#reviewForm {
    background: rgba(255, 255, 255, 0.9);
    border-radius: 15px;
    padding: 20px;
    margin-top: 20px;
}

style.css
/* style.css */

@font-face {
    font-family: 'Mekanik Let';
    src: url('/static/fonts/university-oblique.woff2') format('woff'), url('/static/fonts/university-oblique.ttf') format('truetype');
    font-weight: normal;
    font-style: normal;
}

body,
html {
    margin: 0;
    padding: 0;
    height: 100%;
    font-family: 'Mekanik Let', sans-serif;
    color: #333;
    padding-bottom: 50px;
}

#vanta-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
}

.navbar {
    position: relative;
    z-index: 1;
}

.nav-link {
    color: #333 !important;
    font-weight: bold;
    transition: color 0.3s ease !important;
}

.nav-link:hover {
    color: #ff6f61 !important;
}

.hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding-bottom: 80px;
}

.hero-content {
    position: absolute;
    top: 5vh;
    width: 100%;
    padding: 0 20px;
    max-width: 1200px;
    transform: translateY(0);
    transition: all 1s ease;
}

.logo {
    max-width: 225px;
    margin-bottom: 15px;
    transition: all 0.3s ease;
    transform-origin: center;
    transform: translateY(0);
}

.logo:hover {
    transform: scale(1.02);
}

.logo.move-up {
    transform: translateY(-100px) scale(1);
    margin-bottom: 5px;
}

.title-container {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s ease;
    margin-bottom: 15px;
    text-align: center;
}

.title-container.show {
    opacity: 1;
    transform: translateY(0);
    margin-top: -1.5cm;
}

.about-section {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s ease;
    max-width: 800px;
    margin: 0 auto;
    padding: 10px 0;
}

.about-section.show {
    opacity: 1;
    transform: translateY(0);
    margin-top: -0.5cm;
}

.feature-item {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease;
}

.feature-item.show {
    opacity: 1;
    transform: translateY(0);
}

.feature-card {
    background: rgba(3, 221, 250, 0.096);
    border-radius: 10px;
    padding: 10px;
    margin: 5px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    min-height: 150px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    backdrop-filter: blur(2px);
    transition: all 0.3s ease;
    transform-origin: center;
}

.feature-card:hover {
    transform: scale(1.02);
    z-index: 2;
}

.feature-card h3 {
    font-size: 1.1rem;
    margin-bottom: 8px;
}

.feature-card p {
    font-size: 0.9rem;
    margin-bottom: 0;
}

h1 {
    font-size: 1.6rem;
    margin-bottom: 10px !important;
}

h2 {
    font-size: 1.3rem;
    margin-bottom: 15px !important;
    text-align: center;
}

footer {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 50px;
    background: #B0C4DE;
    color: #333;
    text-align: center;
    padding: 5px 0;
    z-index: 1;
}

.contact-section {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s ease;
    max-width: 600px;
    margin: 0 auto;
    padding: 15px 0;
    text-align: center;
}

.contact-section.show {
    opacity: 1;
    transform: translateY(0);
    margin-top: -0.5cm;
}

.form-title {
    font-size: 1.1rem;
    margin-bottom: 15px;
    text-align: center;
}

.form-control {
    width: 100%;
    padding: 12px 15px;
    margin: 0 auto 8px;
    border: 1px solid #ddd;
    border-radius: 25px;
    background: rgba(211, 237, 252, 0.89);
    backdrop-filter: blur(2px);
    max-width: 280px;
    display: inline-block;
    transition: all 0.3s ease !important;
}

.form-control:hover {
    transform: scale(1.02);
}

.submit-btn {
    width: auto;
    min-width: 200px;
    padding: 12px 30px;
    margin: 15px auto 0;
    background: #2faee9;
    color: white;
    border: none;
    border-radius: 30px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: block;
}

.submit-btn:hover {
    background: #4163fc;
    transform: translateY(-2px);
}

@media (max-width: 768px) {
    .row.g-2>[class^="col-"] {
        margin-bottom: 8px;
    }
    .form-control {
        max-width: 100%;
        display: block;
        margin: 0 auto 8px;
    }
    .submit-btn {
        width: 100%;
        padding: 12px;
    }
    .logo {
        max-width: 200px;
    }
    .logo.move-up {
        transform: translateY(-40px);
    }
    .feature-card {
        min-height: 130px;
    }
    .feature-card h3 {
        font-size: 1rem;
    }
    .feature-card p {
        font-size: 0.85rem;
    }
    .title-container.show {
        margin-top: -0.7cm;
    }
}

@media (max-height: 700px) {
    .hero-content {
        top: 2vh;
        position: relative;
        padding-bottom: 100px;
    }
    .logo {
        max-width: 180px;
    }
    .logo.move-up {
        transform: translateY(-60px);
    }
    h1 {
        font-size: 1.3rem;
    }
    h2 {
        font-size: 1.1rem;
    }
    .feature-card {
        min-height: 120px;
        padding: 8px;
    }
    .contact-section {
        padding: 10px 15px;
    }
    .form-title {
        font-size: 0.95rem;
    }
    .form-control {
        padding: 10px 12px;
    }
    .title-container.show {
        margin-top: -0.5cm;
    }
}

script.js
function initVanta() {
    return VANTA.CLOUDS({
        el: "#vanta-bg",
        mouseControls: true,
        touchControls: true,
        gyroControls: false,
        minHeight: 200.00,
        minWidth: 200.00,
        skyColor: 0x7A869A,
        cloudColor: 0xB0C4DE,
        cloudShadowColor: 0x5A5F69,
        sunColor: 0x7A869A,
        sunGlareColor: 0x7A869A,
        sunlightColor: 0x7A869A,
        speed: 0.5
    });
}

document.addEventListener("DOMContentLoaded", function() {
    const vantaEffect = initVanta();

    const checkScroll = () => {
        const contentHeight = document.documentElement.scrollHeight;
        const viewportHeight = window.innerHeight;
        document.body.style.overflowY = contentHeight > viewportHeight ? 'auto' : 'hidden';
    };

    checkScroll();
    window.addEventListener('resize', checkScroll);

    // Анимация элементов
    setTimeout(() => {
        document.querySelectorAll('.feature-item').forEach((item, index) => {
            setTimeout(() => item.classList.add('show'), index * 100);
        });
    }, 500);
});
// Обработка новостей
document.querySelectorAll('.news-card').forEach(card => {
    card.addEventListener('click', function() {
        // Анимация и отображение полного текста
        this.classList.add('expanded');
        document.body.style.overflow = 'hidden';
    });
});

// Обработка фотоотчетов
document.querySelectorAll('.photo-report-card').forEach(card => {
    card.addEventListener('click', function() {
        const images = JSON.parse(this.dataset.images);
        const carousel = createCarousel(images);
        document.querySelector('#photoCarouselModal .modal-body').innerHTML = carousel;
        new bootstrap.Modal(document.getElementById('photoCarouselModal')).show();
    });
});

function createCarousel(images) {
    let items = '';
    images.forEach((img, index) => {
        items += `
        <div class="carousel-item ${index === 0 ? 'active' : ''}">
            <img src="${img}" class="d-block w-100" alt="Фотоотчет">
        </div>`;
    });

    return `
    <div id="photoCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">${items}</div>
        <button class="carousel-control-prev" type="button" data-bs-target="#photoCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#photoCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
        </button>
    </div>`;
}
base.html
{% load static %}
<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %} - Художественная школа "Щука"</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="{% static 'css/style.css' %}" rel="stylesheet"> {% block head %}{% endblock %}
</head>

<body>
    <div id="vanta-bg"></div>

    <nav class="navbar navbar-expand-lg navbar-light bg-transparent">
        <div class="container">
            <a class="navbar-brand" href="/">Художественная школа "Щука"</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Главная</a></li>
                    <li class="nav-item"><a class="nav-link" href="/reviews/">Отзывы</a></li>
                    <li class="nav-item"><a class="nav-link" href="/news/">Новости</a></li>
                    <li class="nav-item"><a class="nav-link" href="/photo_reports/">Фотоотчеты</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="main-content">
        {% block content %}{% endblock %}
    </div>

    <footer>
        <p>&copy; {% now "Y" %} Художественная школа Щука. Все права защищены.</p>
    </footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r134/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/vanta/0.5.24/vanta.clouds.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{% static 'js/script.js' %}"></script>
    {% block scripts %}{% endblock %}
</body>

</html>

index.html
{% extends 'main/base.html' %} {% load static %} {% block title %}Главная{% endblock %} {% block content %}
<div class="hero-section">
    <div class="hero-content text-center">
        <img src="{% static 'img/logo.png' %}" alt="Логотип" class="logo">

        <div class="title-container show">
            <h1 class="mb-3">Художественная школа "Щука" в Калуге</h1>
            <p class="lead mb-5">Частная художественная школа с академическим подходом к преподаванию</p>
        </div>

        <div class="about-section show">
            <h2 class="mb-4">О школе:</h2>
            <div class="row">
                <div class="col-md-6">
                    <div class="feature-card feature-item show">
                        <h3>Занимаемся с детьми от 4 до 18 лет</h3>
                        <p>Для разного возраста разные группы и предметы</p>
                    </div>
                    <div class="feature-card feature-item show">
                        <h3>Присоединиться можно в любое время года</h3>
                        <p>Индивидуальная программа для каждого нового ученика</p>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="feature-card feature-item show">
                        <h3>Поступление без экзаменов</h3>
                        <p>Пробное занятие для подбора комфортной группы</p>
                    </div>
                    <div class="feature-card feature-item show">
                        <h3>Интересные мероприятия</h3>
                        <p>Праздники, экскурсии, поездки</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="contact-section show">
            <div class="contact-form">
                <h3 class="form-title">Руководитель школы ответит на любые вопросы о занятиях</h3>
                <form>
                    <div class="row g-2">
                        <div class="col-md-6">
                            <input type="text" class="form-control" placeholder="Ваше имя" required>
                        </div>
                        <div class="col-md-6">
                            <input type="tel" class="form-control" placeholder="Ваш телефон" required>
                        </div>
                    </div>
                    <button type="submit" class="submit-btn mt-3">Отправить</button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}

news.html
<!-- templates/main/news.html -->
{% extends 'main/base.html' %} {% load static %} {% block title %}Новости{% endblock %} {% block head %}
<link href="{% static 'css/news.css' %}" rel="stylesheet"> {% endblock %} {% block content %}
<div class="container my-5 pt-5" style="margin-top: -1.5cm;">
    <h2 class="text-center mb-4 title-animate">Новости</h2>
    <div class="row">
        {% for news_item in news %}
        <div class="col-12 mb-4">
            <div class="card news-card" data-id="{{ news_item.id }}">
                <div class="row g-0">
                    <div class="col-md-4">
                        <img src="{{ news_item.image.url }}" class="img-fluid news-image" alt="{{ news_item.title }}">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h3 class="card-title">{{ news_item.title }}</h3>
                            <p class="card-text">{{ news_item.content|truncatechars:200 }}</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

<!-- Модальное окно для полной новости -->
<div class="modal fade" id="newsModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-body" id="newsModalContent"></div>
        </div>
    </div>
</div>
{% endblock %} {% block scripts %}
<script>
    document.addEventListener("DOMContentLoaded", function() {
        document.querySelectorAll('.news-card').forEach(card => {
            card.addEventListener('click', function() {
                const newsId = this.dataset.id;
                fetch(`/news/${newsId}/`)
                    .then(response => response.text())
                    .then(html => {
                        document.getElementById('newsModalContent').innerHTML = html;
                        new bootstrap.Modal(document.getElementById('newsModal')).show();
                    });
            });
        });
    });
</script>
{% endblock %}
photo_reports.html
<!-- templates/main/photo_reports.html -->
{% extends 'main/base.html' %} {% load static %} {% block title %}Фотоотчеты{% endblock %} {% block content %}
<div class="container my-5 pt-5" style="margin-top: -1.5cm;">
    <h2 class="text-center mb-4 title-animate">Фотоотчеты</h2>
    <div class="row">
        {% for report in photo_reports %}
        <div class="col-12 mb-4">
            <div class="card photo-report-card" data-id="{{ report.id }}">
                <div class="row g-0">
                    <div class="col-md-4">
                        <img src="{{ report.main_image.url }}" class="img-fluid report-image" alt="{{ report.title }}">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h3>{{ report.title }}</h3>
                            <p>{{ report.description }}</p>
                            <small class="text-muted">{{ report.created_at }}</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

<!-- Модальное окно для карусели -->
<div class="modal fade" id="photoCarouselModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-body" id="carouselContainer"></div>
        </div>
    </div>
</div>
{% endblock %} {% block scripts %}
<script>
    document.addEventListener("DOMContentLoaded", function() {
        document.querySelectorAll('.photo-report-card').forEach(card => {
            card.addEventListener('click', function() {
                const reportId = this.dataset.id;
                fetch(`/photo_reports/${reportId}/carousel/`)
                    .then(response => response.text())
                    .then(html => {
                        document.getElementById('carouselContainer').innerHTML = html;
                        new bootstrap.Modal(document.getElementById('photoCarouselModal')).show();
                    });
            });
        });
    });
</script>
{% endblock %}


reviews.html

<!-- templates/main/reviews.html -->
{% extends 'main/base.html' %} {% load static %} {% block title %}Отзывы{% endblock %} {% block head %}
<link href="{% static 'css/reviews.css' %}" rel="stylesheet"> {% endblock %} {% block content %}
<div class="container my-5 pt-5" style="margin-top: -1.5cm;">
    <h2 class="text-center mb-4 title-animate">Отзывы</h2>

    <div id="reviewCarousel" class="carousel slide mb-5" data-bs-ride="carousel">
        <div class="carousel-inner">
            {% for review in reviews %}
            <div class="carousel-item {% if forloop.first %}active{% endif %}">
                <div class="review-card">
                    <h5>{{ review.name }}</h5>
                    <p>{{ review.text }}</p>
                    <small class="text-muted">{{ review.created_at }}</small>
                </div>
            </div>
            {% endfor %}
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#reviewCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Предыдущий</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#reviewCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Следующий</span>
        </button>
    </div>

    <div class="p-4" id="reviewForm" style="margin-bottom: 1.5cm;">
        <h2 class="text-center mb-4">Оставить отзыв</h2>
        <form method="post" class="card p-4">
            {% csrf_token %}
            <div class="mb-3">
                <label for="id_name" class="form-label">Ваше имя</label>
                <input type="text" name="name" class="form-control w-100" required id="id_name">
            </div>
            <div class="mb-3">
                <label for="id_text" class="form-label">Ваш отзыв</label>
                <textarea name="text" class="form-control w-100" required id="id_text" rows="4"></textarea>
            </div>
            <button type="submit" class="btn btn-primary submit-btn w-100">Отправить</button>
        </form>
    </div>
</div>
{% endblock %} {% block scripts %}
<script>
    document.addEventListener("DOMContentLoaded", function() {
        // Анимация элементов
        const reviews = document.querySelectorAll('.review-card');
        reviews.forEach((review, index) => {
            setTimeout(() => review.classList.add('show'), index * 150);
        });

        // Настройка карусели
        new bootstrap.Carousel(document.getElementById('reviewCarousel'), {
            interval: 5000,
            wrap: true
        });
    });
</script>
{% endblock %}


manage.py
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'art_school.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()

